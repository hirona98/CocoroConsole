<UserControl x:Class="CocoroConsole.Controls.ApiDocumentationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="800">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="API説明"
                          Margin="0,0,20,20"
                          Padding="10">
                    <StackPanel>
                        <TextBlock TextWrapping="Wrap"
                                   Foreground="#666666">
                            APIを使って外部プログラムから会話入力と定期起床を実行します。（動作未確認）
                        </TextBlock>

                        <Expander x:Name="ConversationApiExpander"
                                  Header="会話API（POST /api/conversation）"
                                  Foreground="#666666"
                                  IsExpanded="False"
                                  Margin="0,10,0,0">
                            <TextBox x:Name="ConversationApiTextBox"
                                     IsReadOnly="True"
                                     BorderThickness="0"
                                     VerticalScrollBarVisibility="Auto"
                                     Foreground="#666666"
                                     Padding="2"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     FontFamily="Consolas"
                                     Margin="0,10,0,0"/>
                        </Expander>

                        <Expander x:Name="WakeApiExpander"
                                  Header="API起床（POST /api/wake）"
                                  Foreground="#666666"
                                  IsExpanded="False"
                                  Margin="0,10,0,0">
                            <TextBox x:Name="WakeApiTextBox"
                                     IsReadOnly="True"
                                     BorderThickness="0"
                                     VerticalScrollBarVisibility="Auto"
                                     Foreground="#666666"
                                     Padding="2"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     FontFamily="Consolas"
                                     Margin="0,10,0,0"/>
                        </Expander>

                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
