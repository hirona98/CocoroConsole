<UserControl x:Class="CocoroConsole.Controls.SpeakerManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <StackPanel>
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="登録済み話者:"
                       VerticalAlignment="Top"
                       Margin="0,0,10,0"/>
            <ListBox Grid.Column="1"
                     x:Name="SpeakersListBox"
                     MinHeight="80"
                     MaxHeight="220"
                     HorizontalContentAlignment="Stretch"
                     VerticalAlignment="Top">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Stretch"
                              Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="85"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding DisplayName}"
                                     VerticalContentAlignment="Center"
                                     Padding="2"
                                     Margin="0,0,5,0"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding RegistrationStatus}"
                                       VerticalAlignment="Center"
                                       Foreground="#666666"
                                       Margin="5,0"/>
                            <Button Grid.Column="2"
                                    Content="名前変更"
                                    Click="RenameSpeaker_Click"
                                    Tag="{Binding}"
                                    Padding="8,2"
                                    Margin="3,0"/>
                            <Button Grid.Column="3"
                                    Content="再登録"
                                    Click="ReenrollSpeaker_Click"
                                    Tag="{Binding}"
                                    Padding="8,2"
                                    Margin="3,0"/>
                            <Button Grid.Column="4"
                                    Content="登録解除"
                                    Click="UnregisterSpeaker_Click"
                                    Tag="{Binding}"
                                    Style="{StaticResource DeleteButtonStyle}"
                                    Padding="8,2"
                                    Margin="3,0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid x:Name="SpeakerActionsPanel" Margin="0,0,0,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="新規登録:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <TextBox Grid.Column="1"
                     x:Name="NewSpeakerNameBox"
                     Margin="0,0,10,0"
                     VerticalContentAlignment="Center"
                     Padding="2"/>
            <Button Grid.Column="2"
                    x:Name="StartEnrollmentButton"
                    Content="3発話で登録"
                    Click="StartEnrollment_Click"
                    Padding="12,2"/>
            <Button Grid.Column="3"
                    x:Name="CancelEnrollmentButton"
                    Content="登録を中止"
                    Click="CancelEnrollment_Click"
                    IsEnabled="False"
                    Padding="12,2"
                    Margin="6,0,0,0"/>
        </Grid>

        <TextBlock x:Name="EnrollmentStatusText"
                   Foreground="#666666"
                   TextWrapping="Wrap"
                   Margin="120,0,0,14"
                   Text="選択中のマイクへ2秒以上の発話を3回入力すると登録が完了します。"/>

        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="105"/>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="識別しきい値:"
                       VerticalAlignment="Center"/>
            <Slider Grid.Column="1"
                    x:Name="ThresholdSlider"
                    Minimum="0.1"
                    Maximum="0.9"
                    TickFrequency="0.05"
                    IsSnapToTickEnabled="True"
                    TickPlacement="BottomRight"
                    ValueChanged="ThresholdSlider_ValueChanged"
                    Margin="10,0"/>
            <TextBlock Grid.Column="2"
                       x:Name="ThresholdValueText"
                       Foreground="Gray"
                       FontSize="11"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"
                       TextAlignment="Left"/>
        </Grid>
    </StackPanel>
</UserControl>
