<Window x:Class="CocoroConsole.Windows.FactResolutionViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="判断トレースビューアー"
        Width="1000"
        Height="900"
        WindowStartupLocation="CenterOwner"
        Icon="/Resource/logo.ico">

        <Grid>
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#F5F5F5"
                        BorderBrush="#E0E0E0"
                        BorderThickness="0,0,0,1"
                        Padding="10">
                        <StackPanel Orientation="Horizontal">
                                <Button x:Name="RefreshButton"
                                        Content="一覧を更新"
                                        Width="110"
                                        Height="28"
                                        Margin="0,0,10,0"
                                        Click="RefreshButton_Click"/>
                                <Button x:Name="ReloadTraceButton"
                                        Content="選択中を再読込"
                                        Width="120"
                                        Height="28"
                                        Margin="0,0,16,0"
                                        Click="ReloadTraceButton_Click"/>
                                <TextBlock Text="最近の判断と根拠を inspection API から表示します。"
                                           VerticalAlignment="Center"
                                           Foreground="#444444"/>
                        </StackPanel>
                </Border>

                <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="320"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                BorderBrush="#E0E0E0"
                                BorderThickness="0,0,1,0"
                                Padding="8">
                                <DataGrid x:Name="CycleListDataGrid"
                                          AutoGenerateColumns="False"
                                          IsReadOnly="True"
                                          SelectionMode="Single"
                                          SelectionChanged="CycleListDataGrid_SelectionChanged"
                                          HeadersVisibility="Column"
                                          GridLinesVisibility="Horizontal"
                                          AlternatingRowBackground="#F9F9F9">
                                        <DataGrid.Columns>
                                                <DataGridTextColumn Header="開始"
                                                                    Binding="{Binding StartedAtDisplay, Mode=OneWay}"
                                                                    Width="90"/>
                                                <DataGridTextColumn Header="きっかけ"
                                                                    Binding="{Binding TriggerKind, Mode=OneWay}"
                                                                    Width="110"/>
                                                <DataGridTextColumn Header="結果"
                                                                    Binding="{Binding ResultKind, Mode=OneWay}"
                                                                    Width="55"/>
                                                <DataGridTextColumn Header="失敗"
                                                                    Binding="{Binding FailedDisplay, Mode=OneWay}"
                                                                    Width="40">
                                                        <DataGridTextColumn.ElementStyle>
                                                                <Style TargetType="TextBlock">
                                                                        <Setter Property="HorizontalAlignment"
                                                                                Value="Center"/>
                                                                        <Setter Property="TextAlignment"
                                                                                Value="Center"/>
                                                                </Style>
                                                        </DataGridTextColumn.ElementStyle>
                                                        <DataGridTextColumn.HeaderStyle>
                                                                <Style TargetType="DataGridColumnHeader">
                                                                        <Setter Property="HorizontalContentAlignment"
                                                                                Value="Center"/>
                                                                </Style>
                                                        </DataGridTextColumn.HeaderStyle>
                                                </DataGridTextColumn>
                                        </DataGrid.Columns>
                                </DataGrid>
                        </Border>

                        <Grid Grid.Column="2"
                              Margin="0,0,0,0">
                                <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0"
                                        Padding="8,0,8,8">
                                        <StackPanel>
                                                <TextBlock x:Name="SelectedCycleTitleTextBlock"
                                                           Text="cycle 未選択"
                                                           FontSize="16"
                                                           FontWeight="SemiBold"
                                                           Margin="0,0,0,4"/>
                                                <TextBlock x:Name="SelectedCycleMetaTextBlock"
                                                           Text=""
                                                           Foreground="#555555"/>
                                        </StackPanel>
                                </Border>

                                <TabControl Grid.Row="1">
                                        <TabItem Header="要点">
                                                <Grid Margin="8">
                                                        <TextBox x:Name="OverviewTextBox"
                                                                 IsReadOnly="True"
                                                                 AcceptsReturn="True"
                                                                 VerticalScrollBarVisibility="Auto"
                                                                 HorizontalScrollBarVisibility="Auto"
                                                                 FontFamily="Meiryo UI"
                                                                 TextWrapping="Wrap"/>
                                                </Grid>
                                        </TabItem>
                                        <TabItem Header="根拠の詳細">
                                                <Grid Margin="8">
                                                        <TextBox x:Name="FactResolutionTraceTextBox"
                                                                 IsReadOnly="True"
                                                                 AcceptsReturn="True"
                                                                 VerticalScrollBarVisibility="Auto"
                                                                 HorizontalScrollBarVisibility="Auto"
                                                                 FontFamily="Consolas"
                                                                 TextWrapping="NoWrap"/>
                                                </Grid>
                                        </TabItem>
                                        <TabItem Header="想起の詳細">
                                                <Grid Margin="8">
                                                        <TextBox x:Name="RecallTraceTextBox"
                                                                 IsReadOnly="True"
                                                                 AcceptsReturn="True"
                                                                 VerticalScrollBarVisibility="Auto"
                                                                 HorizontalScrollBarVisibility="Auto"
                                                                 FontFamily="Consolas"
                                                                 TextWrapping="NoWrap"/>
                                                </Grid>
                                        </TabItem>
                                        <TabItem Header="活動の詳細">
                                                <Grid Margin="8">
                                                        <TextBox x:Name="ActivityTraceTextBox"
                                                                 IsReadOnly="True"
                                                                 AcceptsReturn="True"
                                                                 VerticalScrollBarVisibility="Auto"
                                                                 HorizontalScrollBarVisibility="Auto"
                                                                 FontFamily="Consolas"
                                                                 TextWrapping="NoWrap"/>
                                                </Grid>
                                        </TabItem>
                                        <TabItem Header="全データ">
                                                <Grid Margin="8">
                                                        <TextBox x:Name="CycleTraceTextBox"
                                                                 IsReadOnly="True"
                                                                 AcceptsReturn="True"
                                                                 VerticalScrollBarVisibility="Auto"
                                                                 HorizontalScrollBarVisibility="Auto"
                                                                 FontFamily="Consolas"
                                                                 TextWrapping="NoWrap"/>
                                                </Grid>
                                        </TabItem>
                                </TabControl>
                        </Grid>
                </Grid>

                <Border Grid.Row="2"
                        Background="#F5F5F5"
                        BorderBrush="#E0E0E0"
                        BorderThickness="0,1,0,0"
                        Padding="10,6">
                        <TextBlock x:Name="StatusTextBlock"
                                   Text="inspection API に接続して cycle trace を読み込みます。"/>
                </Border>
        </Grid>
</Window>
