<Window x:Class="CocoroConsole.Windows.ConnectionSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OtomeKairo接続先設定"
        Width="520"
        Height="300"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="OtomeKairoへ接続できるサーバーURLを指定してください。"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="サーバーURL:"
                       VerticalAlignment="Center"/>
            <ComboBox x:Name="ServerUrlComboBox"
                      Grid.Column="1"
                      IsEditable="True"
                      IsTextSearchEnabled="True"
                      StaysOpenOnEdit="True"
                      Padding="4"/>
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="形式: https://&lt;host&gt;:&lt;port&gt;"
                   Foreground="#666666"
                   Margin="110,6,0,0"/>

        <TextBlock x:Name="StatusText"
                   Grid.Row="3"
                   TextWrapping="Wrap"
                   Foreground="#C62828"
                   Margin="0,16,0,12"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="ConnectButton"
                    Content="接続"
                    Width="90"
                    IsDefault="True"
                    Click="ConnectButton_Click"/>
            <Button x:Name="CancelButton"
                    Content="キャンセル"
                    Width="90"
                    Margin="10,0,0,0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
